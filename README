Our code uses processes instead of threads for parallelism. The reason we
chose this is because with threads we could not find a way to sync with
them without always blocking. We limited our program to 10 total processes
that handle all the work. The way it works is that we can service 5 clients
at any given time. For each client, 

